# Handoff git history — DSD P0 corrective round
# HEAD at generation: 4156870

4156870 Corrective brief: P0 analyzer carrier (ffmpeg f64-W64 decode defect)
385a914 Apply DSD Reference P0 implementation (model corrected-v6 bundle)
9064fba Cut DSD P0 handoff: refresh manifest + git history
cfb90ef Bump sox_ng to 14.8.0.1 (barstoolbluz/sox_ng@324b8cf)
7988eeb Re-scope DSD round: P0 commission replaces R1-R7 revision request
4bb6ae4 DSD design round: adopt returned design brief + revision request R1-R7
df73ace Cut DSD commission handoff: line-ref refresh + manifest + git history
2b03901 Counted completion re-invocation supersedes a stalled batch (audit MEDIUM-1)
26e640d Apply round-4 bundle: completion authority, m4a routing, RG parity, selection UX
0d6ffbd Commission brief: DSD->PCM Reference pathway + Manual/Expert workflow design
3a0643b Brief: m4a custom-tag routing, legacy completion authority, RG gating/parity, editor & DSF residuals
e3a8bd4 Audit fixes for the round-2 apply: freeform invocation pin, Browse create focus, deferred secret retirement, listing slot guard
5fa4831 Apply reasoning-model round-2 bundle: DSF in-place completion, custom-tag survival, completion authority, contrast, Browse create (P0-P2)
0766d34 Brief: DSF in-place completion, custom-tag survival, overlay authority, selection contrast, Browse create
e958d74 Verification fixes: ImportCue guards the parked cue-preview session too; stale doc line
02d67f7 Re-audit fixes for 29abba3: parked-session ImportCue bypass, weak pins, doc honesty
29abba3 Audit fixes for the v6 apply: identity demotion, editor mouse-path corruption, cascade ordering
4b07d6a Apply reasoning-model corrective-v6 bundle: DSF write perf, encrypted-file secrets, MB/queue routing, editor/browse UX (P0+P1+P2)
fa1c1f9 Follow-up brief: DSF write perf, secrets policy, MB/queue routing, editor & browse UX
4d3107a Apply reasoning-model v7 bundle: DSF tagging, source pills, RG policy, GNUDB/editor hardening, secrets (R1-R11, F1-F3, H1-H3)
ac67843 Second audit pass of big brief: 6 flags fixed
9a04c01 Big brief: DSF tagging, source pills, RG policy, GNUDB/editor hardening, secrets
afffe61 Audit fixes for the local round: editor degrade hardening, cue INDEX honesty, archive DSD facts
bdb0a43 Local fixes: DSD/DSF conversion chain, published-cue poisoning, tag keys
6a56090 Fix the local known-issues items (doc items 1-3, 5, 7-9a + nits)
b46586c Known issues at G-round push: audited breakage shipping deliberately
7eb466e Lossy CUE images: measure the tail decode instead of trusting the header
23ce692 Apply G-round: Source-depth policy, shared classifier, MB/GNUDB identity, T/S sweeps
5a0870c Brief audit fixes: name flipped pins, tri-state data source, real routes
ae6a7bb Brief: apply-round audit findings (Source-depth policy, float class, MB hatches)

===== FULL DIFF OF 385a914 (P0 apply) STAT ONLY + 538c1e8 =====
commit 385a9149db77d9a3620890ef87f9048a7e8155b8
Author: stephen swoyer <steve@flox.dev>
Date:   Sun Jul 19 21:06:41 2026 -0400

    Apply DSD Reference P0 implementation (model corrected-v6 bundle)
    
    Full P0 Reference DSD->PCM implementation from
    tonepoet_full_tree_9064fba_dsd_reference_p0_implementation_corrected_v6:
    tonepoet-pipeline/src/dsd_reference.rs (policy registry
    sox_ng_14_8_0_1_v3 qualification_candidate, profiles, DbNano, strict
    loudnorm parser, semantic plan hashing), manifest v2 route/track
    identities, DST lossless-decode front-end with in-tree raw fixtures +
    provenance + build-time hashing (sacd-rs build.rs), planner operations
    + fail-closed matrix, native-v2 settings serde/migration, TUI/CLI/
    preset exposure (runtime exposure disabled pending promotion), flake
    policy-owned tool identities baked into the binary, and the
    dsd_reference_qualification tool-gated target.
    
    Apply-side corrections (13, all mechanical boundary class): \u{...}
    escape forms x2, missing use std::io, use-after-move clones x3,
    matches! array-repeat pattern -> if guard, serde_json Map .pointer ->
    chained get x7 sites, DbNano/Sha256Digest path qualification x2,
    containers() -> available_containers(), moved target_format ->
    precomputed bool, struct-shorthand slip; plus warning cleanup (unused
    cfg-gated serde imports, allow(dead_code) on the deny_unknown_fields
    grammar struct).
    
    Gate: workspace suite green; zero cold warnings; tool-gated
    qualification 1/2 — complete_p0_reference_qualification_report fails
    on a REAL toolchain defect, adjudicated not-a-code-bug: ffmpeg's w64
    demuxer mis-scales sox-written plain-IEEE_FLOAT f64 W64 by exactly
    2^31 (+186.64 dB); ffmpeg-written extensible f64 w64 reads fine; sox
    reads its own back exactly. The strict parser rejected the garbage and
    failed closed as designed. Analyzer-carrier correction goes back to
    the model with the empirical matrix (f32-w64 OK, f64-wav OK, f64-aiff
    OK, f64-caf broken, rf64 unwritable by sox).
    
    Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>

 build.rs                                                                    |    9 +
 crates/sacd-rs/Cargo.toml                                                   |    3 +
 crates/sacd-rs/build.rs                                                     |  132 +
 crates/sacd-rs/src/dsd_file/mod.rs                                          |    3 +-
 crates/sacd-rs/src/dsd_file/ops.rs                                          |   30 +
 crates/sacd-rs/src/dst/decoder.rs                                           |   33 +
 crates/sacd-rs/src/dst/fixtures/P0_PROVENANCE.json                          |  204 ++
 crates/sacd-rs/src/dst/fixtures/P0_SHA256SUMS                               |   24 +
 crates/sacd-rs/src/dst/fixtures/generate_p0_raw_fixtures.py                 |   35 +
 crates/sacd-rs/src/dst/fixtures/raw_dsd128_mono.dsd.bin                     |  Bin 0 -> 9408 bytes
 crates/sacd-rs/src/dst/fixtures/raw_dsd128_mono.dst.bin                     |  Bin 0 -> 9409 bytes
 crates/sacd-rs/src/dst/fixtures/raw_dsd128_stereo.dsd.bin                   |  Bin 0 -> 18816 bytes
 crates/sacd-rs/src/dst/fixtures/raw_dsd128_stereo.dst.bin                   |  Bin 0 -> 18817 bytes
 crates/sacd-rs/src/dst/fixtures/raw_dsd256_mono.dsd.bin                     |  Bin 0 -> 18816 bytes
 crates/sacd-rs/src/dst/fixtures/raw_dsd256_mono.dst.bin                     |  Bin 0 -> 18817 bytes
 crates/sacd-rs/src/dst/fixtures/raw_dsd256_stereo.dsd.bin                   |  Bin 0 -> 37632 bytes
 crates/sacd-rs/src/dst/fixtures/raw_dsd256_stereo.dst.bin                   |  Bin 0 -> 37633 bytes
 crates/sacd-rs/src/dst/fixtures/raw_dsd64_6ch.dsd.bin                       |  Bin 0 -> 28224 bytes
 crates/sacd-rs/src/dst/fixtures/raw_dsd64_6ch.dst.bin                       |  Bin 0 -> 28225 bytes
 crates/sacd-rs/src/dst/fixtures/raw_dsd64_mono.dsd.bin                      |  Bin 0 -> 4704 bytes
 crates/sacd-rs/src/dst/fixtures/raw_dsd64_mono.dst.bin                      |  Bin 0 -> 4705 bytes
 crates/sacd-rs/src/dst/fixtures/verify_p0_raw_oracle.py                     |   38 +
 crates/sacd-rs/src/extract.rs                                               |  255 +-
 crates/sacd-rs/src/lib.rs                                                   |   22 +-
 docs/brief_dsd_reference_p0_implementation.md                               | 1643 +++++++++++++
 docs/brief_dsd_reference_p0_policy_v2_amendment.md                          |   50 +
 docs/brief_dsd_reference_p0_policy_v3_amendment.md                          |   96 +
 docs/handoff_dsd_reference_p0_corrected_v3.md                               |  113 +
 docs/handoff_dsd_reference_p0_corrected_v4.md                               |   83 +
 docs/handoff_dsd_reference_p0_corrected_v5.md                               |  100 +
 docs/handoff_dsd_reference_p0_corrected_v6.md                               |   51 +
 docs/handoff_dsd_reference_p0_corrective_round.md                           |    5 +
 docs/handoff_dsd_reference_p0_second_pass.md                                |    5 +
 docs/handoff_manifest.txt                                                   |  251 +-
 flake.nix                                                                   |   25 +-
 src/convert/formats.rs                                                      |   63 +
 src/convert/pipeline/errors.rs                                              |    2 +
 src/convert/pipeline/manifest.rs                                            |  654 ++++-
 src/convert/pipeline/manifest_builder.rs                                    |  277 ++-
 src/convert/pipeline/plan_bridge.rs                                         |  326 ++-
 src/convert/pipeline/rerun.rs                                               |  207 +-
commit 41568708f85efe673971df4ea359a818fbacf2de
Author: stephen swoyer <steve@flox.dev>
Date:   Sun Jul 19 21:22:42 2026 -0400

    Corrective brief: P0 analyzer carrier (ffmpeg f64-W64 decode defect)
    
    Post-apply, post-audit commission: ffmpeg's w64 demuxer mis-decodes
    sox-written plain-IEEE_FLOAT f64 W64 by exactly 2^31; strict parser
    failed closed as designed. Carries the measured carrier matrix, two
    candidate resolutions (streamed exact WAV re-container vs f32
    measurement view with bounded error), new-policy-ID requirements, and
    the four audit LOWs as optional hardening. Audit of 385a914: zero
    confirmed HIGH/MEDIUM.
    
    Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>

diff --git a/docs/brief_dsd_reference_p0_corrective_analyzer_carrier.md b/docs/brief_dsd_reference_p0_corrective_analyzer_carrier.md
new file mode 100644
index 0000000..bec0eb6
--- /dev/null
+++ b/docs/brief_dsd_reference_p0_corrective_analyzer_carrier.md
@@ -0,0 +1,107 @@
+# Corrective Brief: P0 Reference Analyzer Carrier (post-apply, post-audit)
+
+**Status:** your corrected-v6 P0 implementation is applied at `385a914`,
+compiled, and gated: full workspace suite green, zero cold warnings, 13
+mechanical apply-side compile fixes (recorded in the commit message; none
+touch design logic). The three-lane adversarial audit returned **zero
+confirmed HIGH/MEDIUM findings** — legacy argv byte-identity, manifest
+route segregation, the promotion gate, serde rejection surfaces, DbNano
+arithmetic, policy immutability, and plan-hash stability all survived
+hostile review. One tool-gated qualification test fails on a **real
+toolchain defect in your frozen analyzer contract**. This brief carries the
+empirical evidence and commissions the correction.
+
+## The defect (measured, reproducible)
+
+`complete_p0_reference_qualification_report` fails at the true-peak parse:
+`Reference input_tp is outside -1000 to +100 dBTP`. Root cause, isolated on
+the pinned toolchain (sox_ng 14.8.0.1 @324b8cf, ffmpeg 7.1 from the flake):
+
+- **ffmpeg's W64 demuxer mis-decodes sox-written 64-bit-float W64 by
+  exactly 2^31** (+186.64 dB): a −20 dBFS sine measures
+  `input_tp = 166.64` and astats peak 166.64 dB. The scaling factor is
+  exact: 20·log10(2^31) = 186.64.
+- Header trigger: sox writes the fmt chunk with the plain
+  `WAVE_FORMAT_IEEE_FLOAT (0x0003)` tag at 64 bits; ffmpeg's own W64 muxer
+  writes `WAVE_FORMAT_EXTENSIBLE (0xFFFE)` with the float subformat GUID,
+  and ffmpeg reads its own file correctly. ffprobe identifies both as
+  `pcm_f64le`; the corruption is in decode scaling, not codec detection.
+- **Your strict parser caught the garbage and failed closed** — the range
+  check worked exactly as designed. This is the §3.2 contingency your
+  design brief anticipated: "If that pin fails, the policy must select a
+  different analyzer-readable exact carrier."
+
+Empirical carrier matrix (−20 dBFS sine fixtures, this exact toolchain):
+
+```text
+sox f64 → W64  → ffmpeg loudnorm:  input_tp 166.64   BROKEN (+2^31 scale)
+sox f32 → W64  → ffmpeg loudnorm:  input_tp -20.00   correct
+sox f64 → WAV  → ffmpeg loudnorm:  input_tp -20.00   correct (RIFF 4 GiB ceiling)
+sox f64 → AIFF → ffmpeg loudnorm:  input_tp -20.00   correct (4 GiB-class ceiling)
+sox f64 → CAF  → ffmpeg loudnorm:  input_tp 166.64   BROKEN
+sox f64 → RF64:                    sox_ng cannot write RF64
+sox f64 W64 → sox stats readback:  Pk -20.00 exact   render chain UNAFFECTED
+```
+
+The last line matters: sox round-trips its own f64 W64 exactly, so the
+R64 → finalize chain keeps its sample-exactness pin. Only the *analyzer
+view* of R64/QPCM needs a new qualified path.
+
+## Directive
+
+Select and specify the corrected analyzer contract under your own
+append-only policy rules (this changes measurement argv, the analyzer
+identity, and qualification evidence → new policy ID per your §1.2 rules;
+the release was never promoted, so no persisted-authority migration is
+needed — state that explicitly). Candidate resolutions, decide with
+rationale:
+
+1. **Streamed exact re-container for measurement**: pipe the f64 W64
+   carrier through sox as f64 WAV to ffmpeg's stdin
+   (`sox R64.w64 -t wav - | ffmpeg -f wav -i pipe:0 …`). **Verified on
+   this exact toolchain:** the −20 dBFS f64 W64 fixture measures
+   `input_tp = -20.00` through the pipe. Streaming WAV avoids the
+   RIFF on-disk size ceiling (unknown-size header, ffmpeg reads to EOF —
+   the >4 GiB streamed case still needs its own qualification fixture);
+   sox f64→f64 WAV is a container re-wrap, sample-exact. Cost: one extra
+   decode-side pass, no disk. Requires: pinning the pipe semantics
+   (unknown-size RIFF acceptance) in qualification, and deciding how the
+   two-process measurement step fits your PlannedMeasurement single-command
+   shape (a typed two-stage measurement, not a shell pipeline — no shell).
+2. **f32 measurement view with a policy error bound**: render an f32 W64
+   measurement copy (sox f64→f32, correctly read by ffmpeg). f32 mantissa
+   (24-bit) cannot represent the f64 samples exactly, but the added
+   true-peak error is boundable (≤ 2^-24 relative ≈ 0.0000005 dB-scale
+   near full scale) and can be folded into the certified one-sided
+   analyzer uncertainty `Q`/`E` arithmetic. Cost: one extra render-side
+   pass + a new bound derivation; keeps single-command measurement.
+3. Any alternative you can qualify (e.g. teaching the policy to accept an
+   ffmpeg-side `-f w64`-with-forced-decoder workaround only if you can
+   demonstrate correct decode — our probing found none).
+
+Requirements regardless of choice: update the frozen measurement argv and
+parser contract; re-derive the qualification manifest and certification
+JSON; update `tests/dsd_reference_qualification.rs` so
+`complete_p0_reference_qualification_report` passes against real tools;
+record the ffmpeg W64 defect in provenance/docs so a future ffmpeg fix
+does not silently change the measurement path without a policy bump.
+
+## Audit LOWs (optional hardening, fold in if cheap)
+
+- Strict loudnorm report parse: serde_json accepts duplicate keys
+  (last-wins). The one-report-object check narrows exposure; a duplicate
+  `input_tp` key inside the single report would still parse. A manual
+  duplicate-key scan of the extracted report object closes it.
+- `crates/sacd-rs/build.rs`: additionally verify fixtures against
+  `P0_SHA256SUMS` at build time (content-hash corpus ID already fails
+  loudly at test time; this just fails earlier).
+- Manifest serde tags (`route`, snake_case variant names) are load-bearing
+  wire format: add a frozen-string fixture test if not already pinned.
+- SACD ISO preflight double-SHA race: system-level, accepted; document as
+  out-of-threat-model in the module docs.
+
+## Out of scope
+
+Everything else. The P0 surface, deferrals (Manual workflows, in-TUI
+builder, lossy delivery), and the promotion process are unchanged. Do not
+expand scope. Complete-file delivery contract applies.
