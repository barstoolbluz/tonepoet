# Handoff — Round 10 (bundle readme)

**Governing document:** `docs/round10-brief.md`. Read it fully first.
Every mechanism is audit-verified to the line (three overlapping audit
bands plus a fresh-eyes re-verification — ~120 citations checked, plus
a live pipeline repro for §1). Do not re-derive the verified seams,
and do not second-guess the audit-forced decisions recorded inline —
in particular: the §1 single-flip authority fix (the obligations
model already consumes the predicate — audit-confirmed complete), the
§2 fingerprint MODE-SCOPED EMISSION mandate (both alternatives are
forbidden with cited doctrine), the Gesemann exclusion from the
relaxation arm, the dither_overridden depth-reset ruling (intended —
do not "fix" it), the §4 rerun trade-off truths (skip-forever;
duplicate-numbering residue; NO DestinationExists hazard), the §5
TEXTUAL-padded correction and capability-aware preflight compare, and
the §6 five-guards-by-name list.

**Baseline:** branch `hardening` @ 14c9b04 (+ docs commits);
`cargo test --workspace` = 5,354 passed / 0 failed across 56 targets.
Version stays **0.4.4**.

**The acceptance fixture is live:** the user keeps
`~/livetorrents/Supertramp – Even In The Quietest Moments...-1977`
deliberately broken (invalid APEv2 key `&год`). §1.3 states the
outcome the user will verify in the field.

## Scope, in suggested order

1. §1 metadata write authority (the field-blocker): recovery marker +
   predicate flip; combined §1×§4 pin.
2. §2 dither override: dither_explicit plumbing + fingerprint
   mode-scoped emission + gate relaxation at the four tool sites and
   the log gate, with the Gesemann/SSRC/float disclosures.
3. §3 unconditional DSP documentation: three affirmative Settings
   lines + per-track disclosure annotations + the eight test
   rewrites.
4. §4 ordinal filename fallback + doc-contract rewrites.
5. §5 PaddedUnsigned (TEXTUAL correction + preflight fix + full flip
   list incl. the message/helper ripples).
6. §6 ledger items.

## Non-negotiable constraints

- The two dither gate copies (command construction + log) change in
  LOCKSTEP — log claims must always match emitted commands.
- Reference DSD is untouched (Int32 stays fail-closed; policy dither).
- Fingerprints: no existing digest may change for non-explicit
  settings; the sentinel inventory is updated per the mandate.
- §1 scope: fallback-recovered sources ONLY (all-SingleFile
  authoritative writes are fenced).
- §4 writes filenames only — never TRACKNUMBER tags, never log
  ordering; disclosure says "dispatch order".
- NO F-keys; NO emoji/decorative unicode; Ctrl+Q stays quit; version
  0.4.4; never truncate gate output; rounds 5-9 machinery must not
  regress (the DSD Reference qualification suite must stay green —
  audit-verified isolation holds if changes stay in the plugins.rs
  gates + stages.rs log gate).
- Fences: §7 list verbatim (pairing-guard relaxation still awaits
  user field evidence; Custom builder + Paste tags still queued).

## Deliverables

- Overlay bundle (tar.gz, nested dir) with a preimage manifest
  (SHA-256 of the exact base revisions received) covering every
  modified file.
- Engineering report: per-item named pins (§8's minimum list), the
  §2 empirical ffmpeg AND sox s32-dither probe results, the
  fingerprint mode-scoped-emission statement, the §5 TEXTUAL
  resolution + per-backend preflight compare rule stated explicitly,
  disclosed limitations (§4 trade-offs as corrected; SSRC/float/
  Gesemann dither disclosures; lexicographic ordinal caveat), and
  any deviation with rationale.
- `cargo test --workspace` stays green against 5,354/0; new tests
  must FAIL if the behavior they pin regresses.
